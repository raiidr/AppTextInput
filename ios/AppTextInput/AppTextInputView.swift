import React
import UIKit

@objc
public class AppTextInputView: AppTextInputCoreView {
  // The editable text input with inline Lottie emoji. All implementation lives
  // in AppTextInputCoreView; this class exists so the legacy view manager can
  // vend a concrete component named AppTextInputView.
}
